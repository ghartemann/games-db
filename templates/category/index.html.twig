{% extends 'base.html.twig' %}

{% block title %}Genres - {% endblock %}

{% block body %}
    {% for category in categories %}
        <div>
            <h2>{{ category.name }}</h2>
        </div>
    {% endfor %}
{% endblock %}
